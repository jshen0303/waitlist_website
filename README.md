# Website Runbook

1. brew install node, make sure you have npm installed
2. check with npm -v for version
3. git clone the repo
4. cd /path to folder/
5. npm install
6. npm run dev
7. copy localhost port link to browser
